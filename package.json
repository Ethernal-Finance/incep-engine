{
  "name": "inception-game",
  "version": "1.0.0",
  "description": "Construct 3 style game engine for top-down Zelda-like RPG",
  "type": "module",
  "scripts": {
    "dev": "npm run generate-asset-tree && vite",
    "build": "npm run generate-asset-tree && tsc && vite build",
    "preview": "vite preview",
    "generate-manifest": "node scripts/generate-asset-manifest.js",
    "generate-asset-tree": "node scripts/generate-asset-tree.js"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}

